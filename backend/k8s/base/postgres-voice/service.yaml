apiVersion: v1
kind: Service
metadata:
  name: postgres-voice-svc
  namespace: mercadoverde
spec:
  type: ClusterIP
  selector:
    app: postgres-voice
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
