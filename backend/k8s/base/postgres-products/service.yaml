apiVersion: v1
kind: Service
metadata:
  name: postgres-products-svc
  namespace: mercadoverde
spec:
  type: ClusterIP
  selector:
    app: postgres-products
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
