# Puerto donde corre el servicio Auth
PORT=3001

# Configuración de la base de datos (ajústala según tu docker-compose)
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/authdb

# ==== JWT ACCESS TOKENS ====
# Secreto para firmar/verificar access tokens
JWT_SECRET=your_access_secret_key
# Tiempo de vida del access token (puede ser en segundos o formato "15m", "1h")
JWT_ACCESS_TTL=15m

# ==== JWT REFRESH TOKENS ====
# Secreto distinto para refresh tokens (mejor práctica de seguridad)
JWT_REFRESH_SECRET=your_refresh_secret_key
# Tiempo de vida del refresh token
JWT_REFRESH_TTL=7d

# Opcional: identidad del emisor y audiencia (para validaciones más estrictas)
JWT_ISS=mercadoverde-auth
JWT_AUD=mercadoverde-clients

# Entorno de ejecución
NODE_ENV=development
