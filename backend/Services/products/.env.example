# Puerto donde corre el servicio Products
PORT=3003

# Configuración de la base de datos (ajústala según tu docker-compose)
DATABASE_URL=postgresql://admin:admin@db:5432/microservices

# ==== JWT ACCESS TOKENS ====
# Secreto para validar access tokens (DEBE SER EL MISMO que JWT_SECRET en Auth)
JWT_SECRET=your_access_secret_key

# Tiempo de vida del access token (opcional, solo para consistencia en logs o validaciones)
JWT_ACCESS_TTL=15m

# Opcional: identidad del emisor y audiencia (deben coincidir con Auth si las usas allí)
JWT_ISS=mercadoverde-auth
JWT_AUD=mercadoverde-clients

# Entorno de ejecución
NODE_ENV=development
