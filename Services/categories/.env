# Services/categories/.env

# Puerto del servicio Categories
PORT=3004

# Base de datos (elige según dónde corre Postgres)
# Si corres los servicios en tu HOST (como hiciste con 5433):
DATABASE_URL=postgresql://admin:admin@localhost:5433/microservices?schema=public
# Si lo ejecutas en Docker dentro de la misma red que el contenedor "db":
# DATABASE_URL=postgresql://admin:admin@db:5432/microservices?schema=public

# JWT: deben coincidir exactamente con auth/
JWT_SECRET=super_secret_en_dev
JWT_ISS=mercadoverde-auth
JWT_AUD=mercadoverde-clients

NODE_ENV=development
